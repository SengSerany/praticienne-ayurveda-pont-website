---
import Pont from './Pont.astro';

interface Props {
  numero?: string;
  titre: string;
}
const { numero, titre } = Astro.props;
---

<Pont numero={numero} eyebrow="LE PONT" titre={titre}>
  <slot />
</Pont>
